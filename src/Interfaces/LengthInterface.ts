export default interface LengthInterface {

    /**
     * Get the length of the object.
     *
     * @return {number} The length of the object, positive integer.
     */
    get length(): number;
}
